#include <bits/stdc++.h>
using namespace std;

int main()
{

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        vector<int> arr(n);
        for (auto x : arr)
            cin >> x;

        int i=0;
        //right loop 
        while(i<=arr.size()-1){
            if(arr[i]==1)
        }
        
    }
    return 0;
}